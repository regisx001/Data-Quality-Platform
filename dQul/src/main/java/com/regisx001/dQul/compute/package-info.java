/**
 * Compute package for data quality computation engines.
 * Houses the execution engines that run quality rules, validations,
 * and statistical computations against datasets.
 */
package com.regisx001.dQul.compute;
